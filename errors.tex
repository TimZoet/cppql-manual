% Exceptions vs error codes. If program can continue without too many issues, error code (functions that wrap sql api calls return an actual error code, others booleans). Otherwise, exception. Look at return types and noexcept to determine which is which.
% getErrorMessage()